<div class="row">
    <h3>Greeted People</h3>

    <h5>Names of the People</h5>
    <hr>
    <ul>
        {{#each users}}
        <li>
            <a href="/counter/{{this.name_greeted}}">{{this.name_greeted}}</a>
        </li>
        {{/each}}
    </ul>


    <h1> {{messages.info}}</h1>

</div>